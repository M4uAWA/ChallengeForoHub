CREATE TABLE Topico ( id INT AUTO_INCREMENT PRIMARY KEY, titulo VARCHAR(200) NOT NULL, mensaje TEXT NOT NULL, fechaCreacion DATETIME DEFAULT CURRENT_TIMESTAMP, status ENUM('ABIERTO','CERRADO') DEFAULT 'ABIERTO', autor INT, curso INT );